// StatsGrid is now rendered inline in AdminDashboard using real API data.
// This file is kept as a no-op export to avoid import errors during migration.
const StatsGrid = () => null;
export default StatsGrid;
